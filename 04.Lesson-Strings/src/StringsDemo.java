public class StringsDemo {
     static public void main(String[] Args) {

         String word1 = "Baby";
         String word2 = "Apples";
         String resultWord = word1.substring(0, word1.length() / 2) +
                word2.substring(word2.length() / 2);
         System.out.println("1 ======");
         System.out.println(word1 + " + " + word2 + " = " + resultWord);

         String corpus = "Около полудня обыкновенно появляется множество круглых высоких облаков, золотисто-серых, с нежными белыми краями. Подобно островам, разбросанным по бесконечно разлившейся реке, обтекающей их глубоко прозрачными рукавами ровной синевы, они почти не трогаются с места; далее, к небосклону, они сдвигаются, теснятся, синевы между ними уже не видать; но сами они так же лазурны, как небо: они все насквозь проникнуты светом и теплотой. ";

//         for  (int i=0, i < corpus.length(), i++) {
//
//
//
//         }

    }
}

